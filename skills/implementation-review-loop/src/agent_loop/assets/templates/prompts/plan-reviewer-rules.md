<!--
このファイルには、このリポジトリ固有の plan review ルールを書きます。

- ここに書いた内容は plan review 時にプロンプトの
  「## プロジェクト固有レビュールール」セクションとして注入され、
  共通プロンプト (plan-reviewer.md) の一般則より優先されます。
- HTML コメント (この雛形ブロック) は注入前に除去されます。
  コメント以外の記述が無い場合、このファイルは注入されません。
- 書くべきもの: このプロジェクトの CLAUDE.md / 要件定義書に由来する
  「計画書レビューで必ず点検してほしい制約」。共通プロンプトに既にある
  一般則の繰り返しは書かないでください。

書き方の注意:

- 見出しを使う場合は「###」以下のレベルにしてください。「##」で書くと、
  注入先の「## プロジェクト固有レビュールール」セクションの配下ではなく
  同格の見出しになり、ルールの帰属が曖昧になります。
- コメントの外の本文に、HTML コメントの開始マーカー・終了マーカーを
  リテラルとして書かないでください (コード例や mermaid の矢印を含む)。
  マーカーが対になっていないと plan review はエラーで停止し、対になって
  いると間に挟まれた本文がコメントと見なされて除去されます。
- この機能を使わない場合は、このファイルを消すのではなく config
  (.agent-loop/config.json) から planReviewerRules キーを削除してください。
  キーを残したままファイルを削除すると plan review と doctor が失敗します。

記入例 (プロジェクトに合わせて書き換えて、コメントの外に置く):

### 絶対制約 (MVP 優先の対象外)

以下は CLAUDE.md「動かしてはならない実装上の制約」に由来する。該当する
Tool / 出力を扱う Phase の計画書に、実装とテストの両方が含まれていなければ
finding にする。「Phase 2 以降で追加」への持ち越しは認めない。

- 外部作用のある Tool は dry_run=true を既定とし、解除は人間承認を要する
- expected_* フィールドはエージェント向け View から常に秘匿する
- 最終承認の断定表現を出力しない (人間承認必須ケースを明示する)

### AC の対応付け

- 要件定義書 §15 の AC のうち当該 Phase 担当分について、計画書に
  契約テスト / 統合テストの形で達成根拠が明示されているかを点検する

### 共通プリミティブ

- ToolError / Evidence は cross-Phase 共通プリミティブとして
  共通定義を使う。機能ごとの重複定義は finding にする
  (逆に、これらの共通化を「先取り」として弾かない)
-->
